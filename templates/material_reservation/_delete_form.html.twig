<form method="post" action="{{ path('material_reservation_delete_front', {'id': material_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ material_reservation.id) }}">
    <button class="btn">Delete</button>
</form>
