{% extends 'admin/base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1>Materiel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ materiel.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ materiel.description }}</td>
            </tr>
            <tr>
                <th>Disponibility</th>
                <td>{{ materiel.disponibility ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ materiel.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rent_management') }}">back to list</a>

    <a href="{{ path('materiel_edit', {'id': materiel.id}) }}">edit</a>

    {{ include('admin/rent_management/materiel/_delete_form.html.twig') }}
{% endblock %}
