{% extends 'base.html.twig' %}
{% block title %}Reserver{% endblock %}
{% block body %}
    <h1>InscriptionEvent</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ reserver.id }}</td>
        </tr>
        <tr>
            <th>Iduser</th>
            <td>{{ reserver.iduser }}</td>
        </tr>
        <tr>
            <th>Idevent</th>
            <td>{{ reserver.idevent }}</td>
        </tr>
        <tr>
            <th>Nbrplace</th>
            <td>{{ reserver.nbrplace }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('reserver_index') }}">back to list</a>

    <a href="{{ path('reserver_edit', {'id': reserver.id}) }}">edit</a>

    {{ include('reserver/_delete_form.html.twig') }}
{% endblock %}
