{% extends 'base.html.twig' %}

{% block title %}ReservationMateriel{% endblock %}

{% block body %}
    <h1>ReservationMateriel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_materiel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reservation_materiel.Name }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ reservation_materiel.DateCreation ? reservation_materiel.DateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_materiel_index') }}">back to list</a>

    <a href="{{ path('reservation_materiel_edit', {'id': reservation_materiel.id}) }}">edit</a>

    {{ include('renting_front/reservation_materiel/_delete_form.html.twig') }}
{% endblock %}
